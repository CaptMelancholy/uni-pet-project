const DefaultRoutes = {
  default: '/',
  auth: '/join',
  logout: '/logout',
  u: '/u/:username',
  boards: '/boards',
  board: '/board/:id',
  error: '/error/:code',
  success: '/success',
  invite: '/invite/:hash',
  account: '/account',
};

export default DefaultRoutes;

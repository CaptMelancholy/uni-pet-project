import styled from 'styled-components';

export const BoardContainer = styled.div`
  display: flex;
  gap: 16px;
  padding-bottom: 16px;
  overflow-x: auto;
  overflow-y: hidden;
  scrollbar-width: thin;
  flex: 1 1;
  align-items: stretch;
`;

import styled from 'styled-components';

export const CardMarksWrapper = styled.div`
    display: flex;
    gap: 10px;
    flex-direction: column;
    width: 100%;
`;

export const CardMarksList = styled.div`
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
`;